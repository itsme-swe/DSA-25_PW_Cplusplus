#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
  string str = "Welcome";

  reverse(str.begin(), str.end()); // use to reverse the string and part of strings also

  cout << str << endl; // emocleW

  return 0;
}